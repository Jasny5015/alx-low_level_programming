#include<stdio.h>

/**
 * main - Prints from 0 to 9
 *
 * Return: Always return 0 once successful
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf('%d', i);
	}
	printf("\n");
	return (0);
}
