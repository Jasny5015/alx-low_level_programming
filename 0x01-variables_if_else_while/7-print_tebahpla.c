#include<stdio.h>

/**
 * main - Prints alphabet from z to a
 *
 * Return: Alway return 0 once successful
 */

int main(void)
{
	int ch;

	for (ch = 'z'; ch >= 'a'; ch--)
	{
		putchar(ch);
	}
	putchar('\n')
		return (0);
}
